// Example 1: Basic Rules and Queries
// Demonstrates simple rule derivation

// Configuration
@model FHRR(dim=512, seed=42);
@beam(width=50);
@novelty(threshold=0.95);

// Base facts: People
fact person(alice).
fact person(bob).
fact person(carol).

// Rule: Humans are persons
rule human(X) :- person(X).

// Query: Who are the humans?
query human(?)?
